# Copy to `.env` and fill in. Expo exposes EXPO_PUBLIC_* vars to the client bundle.
# These two are safe to ship in a client app (the anon key is protected by RLS).
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=YOUR-SUPABASE-ANON-KEY
